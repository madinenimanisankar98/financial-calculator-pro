# Copy this file to .env and fill in your own values.
# Optional: enables the Gemini-powered "Ask Questions" assistant.
# If left blank, the app automatically falls back to its offline
# knowledge base, so the feature still works without this key.
GEMINI_API_KEY=

# Optional: port for local development (Vercel ignores this in production)
PORT=5000
